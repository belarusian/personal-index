# QA-54: ContentArchiver.archive_old(days_threshold=0) falls back to default 30 days

**Status:** CLOSED (validator close, cycle 328 @ main 15660b0; deep pins re-run green, 102 passed 0 xfail; issue #1693 closed)
**Issue:** #1693
**Deep test:** tests/deep/test_content_archive_adversarial.py::TestArchiverEdgeCases::test_archive_old_zero_days

## Symptom
Calling `archive_old(days_threshold=0)` does not archive items older than now (as the contract implies). Instead, it falls back to the default 30-day threshold because of the falsy-evaluation bug in the threshold selection logic.

## Root Cause
In `personal_index/content_archive/archiver.py:77`: