# QA-68: TimelineEvent/TimelineEntry.from_dict store a non-string timestamp verbatim (docstring promises degrade-to-now-utc)

- **Status:** IMPLEMENTED #1744@b6378a27
- **Component:**
  * `personal_index/content_timeline/timeline_event.py` (`TimelineEvent.from_dict`, lines 96-102)
  * `personal_index/content_timeline/timeline_entry.py` (`TimelineEntry.from_dict`, lines 79-85)
- **Kind:** contract violation (docstring vs code — uncaught `AttributeError` downstream)
- **Issue:** #1729
- **Deep test:** `tests/deep/test_content_timeline_adversarial.py` (3 xfail-strict pins: `test_event_from_dict_nonstring_timestamp_degrades`, `test_entry_from_dict_nonstring_timestamp_degrades`, `test_event_from_dict_none_timestamp_degrades`)

## Symptom

Both `TimelineEvent.from_dict` and `TimelineEntry.from_dict` docstrings state,
for the `timestamp` field:

> ``timestamp`` -> ``data.get("timestamp", now-utc-iso)``; if a string, parsed
> via ``datetime.fromisoformat``; on ``ValueError`` **(or a non-string value)**
> it degrades to ``datetime.now(timezone.utc)``

The code only guards the `ValueError` branch: