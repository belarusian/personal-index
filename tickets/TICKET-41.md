# TICKET-41: Unused variable — `content_dedup.py:324` `title` parameter in `_check_single_item()` is never used

## Title
`title` parameter in `ContentDeduplicator._check_single_item()` is accepted but never used

## Evidence
In `personal_index/content_dedup.py:323-349`:
