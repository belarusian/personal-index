# TICKET-42: Unused variable — `content_categorizer.py:571` `text` parameter in `_build_reasons()` is never used

## Title
`text` parameter in `ContentCategorizer._build_reasons()` is accepted but never used

## Evidence
In `personal_index/content_categorizer.py:571-596`:
