# TICKET-40: Missing docstrings — `models.py:347,351` `to_dict()` and `from_dict()` lack docstrings

## Title
`SchedulerConfig.to_dict()` and `SchedulerConfig.from_dict()` are missing docstrings

## Evidence
In `personal_index/models.py:341-353`:
