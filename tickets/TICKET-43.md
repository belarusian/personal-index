# TICKET-43: Unused variable — `search_index.py:91` `page` in `remove()` is assigned but never used

## Title
`page` variable in `SearchIndex.remove()` is assigned but never used (suppressed with noqa)

## Evidence
In `personal_index/search_index.py:86-101`:
