# Architecture

## System Overview

personal-index is a personal web search engine that scans, filters, and indexes the web based on user-defined interests. The system consists of a CLI interface, a web crawler, a full-text search index, content processing pipeline, and an optional REST API server.

## Component Diagram
